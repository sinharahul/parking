<%= form_for(@car_registration) do |f| %>
  <% if @car_registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_registration.errors.count, "error") %> prohibited this car_registration from being saved:</h2>

      <ul>
      <% @car_registration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :owner %><br>
    <%= f.text_field :owner %>
  </div>
  <div class="field">
    <%= f.label :license %><br>
    <%= f.text_field :license %>
  </div>
  <div class="field">
    <%= f.label :month %><br>
    <%= f.text_field :month %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
